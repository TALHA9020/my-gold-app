{
  "name": "my-app",
  "version": "1.0.0",
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  },
  "scripts": {
    "build": "mkdir -p www && echo 'Build complete'"
  }
}
